# Turing Platform

This is the main repository of the Turing platform.

## Getting Started

Go into your workspace at https://work.turing.bio and run the following command in the terminal:
```bash
clone
```

You should now see a directory named **turing** in the current directory.
