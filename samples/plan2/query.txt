// MATCH (b)-->(c), (a { name: c.name })-->(b)
// RETURN a

// MATCH (b)-->(c), (a)-->(b)
// WHERE a.name = b.name
// RETURN a

MATCH (n)-->(m)-->(p)
WHERE n:Person AND n.name = "Luc" AND n.duration = m.duration OR p.= 25 AND n.age = 20
RETURN a

// MATCH (a)-->(b), (b)-->(c { name: a.name })
// RETURN a
