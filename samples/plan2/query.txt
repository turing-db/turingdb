// MATCH (b)-->(c), (a { name: c.name })-->(b)
// RETURN a

// MATCH (b)-->(c), (a)-->(b)
// WHERE a.name = b.name
// RETURN a

MATCH (a)-[e]->(b), (b)-->(c)
WHERE a:Person AND a.name = "Luc"
RETURN a

// MATCH (a)-->(b), (b)-->(c { name: a.name })
// RETURN a
