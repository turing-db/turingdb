{
  "id": "fail-reads/aggregate-0",
  "name": "fail-reads/aggregate-0",
  "enabled": true,
  "graph": "simpledb",
  "query": "MATCH (n { name: count(n.duration)}) RETURN n",
  "expect": {
    "plan": "ANALYZE ERROR\n-------* Query error\n     1 | MATCH (n { name: count(n.duration)}) RETURN n\n       |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n-------* Invalid use of aggregate expression in this context",
    "result": "ANALYZE ERROR\n-------* Query error\n     1 | MATCH (n { name: count(n.duration)}) RETURN n\n       |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n-------* Invalid use of aggregate expression in this context"
  },
  "tags": [
    "fail",
    "reads",
    "aggregate"
  ]
}
