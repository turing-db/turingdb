{
  "id": "fail-reads/aggregate-1",
  "name": "fail-reads/aggregate-1",
  "enabled": true,
  "graph": "simpledb",
  "query": "MATCH (n) WHERE n.duration = count(n) RETURN n",
  "expect": {
    "plan": "ANALYZE ERROR\n-------* Query error\n     1 | MATCH (n) WHERE n.duration = count(n) RETURN n\n       |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n-------* Invalid use of aggregate expression in this context",
    "result": "ANALYZE ERROR\n-------* Query error\n     1 | MATCH (n) WHERE n.duration = count(n) RETURN n\n       |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n-------* Invalid use of aggregate expression in this context"
  }
}
