{
  "enabled": true,
  "graph": "simpledb",
  "query": "UNWIND [1, 2, 3] AS mylist MATCH (n) RETURN n, mylist",
  "expect": {
    "plan": "ANALYZE ERROR\n-------* Query error\n     1 | UNWIND [1, 2, 3] AS mylist MATCH (n) RETURN n, mylist\n       |        ^^^^^^^^^\n-------* Non-list arguments to UNWIND are not yet supported",
    "result": "ANALYZE ERROR\n-------* Query error\n     1 | UNWIND [1, 2, 3] AS mylist MATCH (n) RETURN n, mylist\n       |        ^^^^^^^^^\n-------* Non-list arguments to UNWIND are not yet supported",
    "resultJson": "{\"error\":\"ANALYZE_ERROR\",\"error_details\":\"-------* Query error\\n     1 | UNWIND [1, 2, 3] AS mylist MATCH (n) RETURN n, mylist\\n       |        ^^^^^^^^^\\n-------* Non-list arguments to UNWIND are not yet supported\"}"
  },
  "tags": [
    "lists",
    "unwind",
    "match",
    "return",
    "cartesian"
  ],
  "write-required": false
}
