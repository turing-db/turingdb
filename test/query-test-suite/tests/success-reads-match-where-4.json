{
  "enabled": true,
  "graph": "simpledb",
  "query": "MATCH (a)-->(x), (b { duration: a.duration })-->(x ), (c)-->(x)\nWHERE (a.name = b.name) OR (b.name = c.name) OR (b.name = x.name)\nRETURN a",
  "expect": {
    "result": "a",
    "resultJson": "{\"header\":{\"column_names\":[\"a\"],\"column_types\":[\"UInt64\"]},\"data\":[[[]]]}"
  },
  "tags": [
    "success",
    "reads",
    "match",
    "where"
  ],
  "write-required": false
}
